version: '3'

services:
  postgres:
    image: postgres:16
    environment:
      - 'POSTGRES_DB=etutor_sql_ddl'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=etutor_sql_ddl'
    ports:
      - '5434:5432'
